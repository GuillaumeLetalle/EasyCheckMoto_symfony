{% extends 'base.html.twig' %}


{% block body %}
    <h1>Contôle Technique :</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Debut</th>
                <th>Fin</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        
        {% for ct in cts %}
        
            <tr>
                <td>{{ ct.debut ? ct.debut|date('d-m-Y H:i:s') : '' }}</td>
                <td>{{ ct.fin ? ct.fin|date('d-m-Y H:i:s') : '' }}</td>
                <td>
                    
                    <a href="{{ path('app_ct_show', {'id': ct.id}) }}">Voir</a>
                  
                    
                    
                    {# <a href="{{ path('app_ct_edit', {'id': ct.id}) }}">Effectuer</a> #}
                   
                </td>
            </tr>
        
       
        {% endfor %}
        
        </tbody>
    </table>

    {# <a href="{{ path('app_ct_new') }}">Create new</a> #}
{% endblock %}
