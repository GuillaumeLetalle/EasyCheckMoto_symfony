<form method="post" action="{{ path('app_c_t_delete', {'id': c_t.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ c_t.id) }}">
    <button class="btn">Delete</button>
</form>
