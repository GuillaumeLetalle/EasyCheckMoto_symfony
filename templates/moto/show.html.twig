{% extends 'base.html.twig' %}

{% block title %}Moto{% endblock %}

{% block body %}
    <h1>Moto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Marque</th>
                <td>{{ moto.marque }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ moto.modele }}</td>
            </tr>
            <tr>
                <th>Cylindree</th>
                <td>{{ moto.cylindree }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ moto.annee }}</td>
            </tr>
            <tr>
                <th>Immatriculation</th>
                <td>{{ moto.immatriculation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moto_edit', {'id': moto.id}) }}">Modifier</a></br>

    <a href="{{ path('app_moto_index' , {'id': app.user.id}) }}">Mon garage</a>

    {{ include('moto/_delete_form.html.twig') }}
{% endblock %}
