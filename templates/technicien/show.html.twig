{% extends 'base.html.twig' %}

{% block body %}

    <table class="table">
        <tbody>
            
            <tr>
                <th>Username</th>
                <td>{{ technicien.username }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ technicien.name }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ technicien.firstname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_technicien_index') }}">Liste Techniciens</a>

    {{ include('technicien/_delete_form.html.twig') }}
{% endblock %}
