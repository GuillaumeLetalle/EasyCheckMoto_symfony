{% extends 'base.html.twig' %}


{% block body %}
    <h1>Techniciens</h1>

    <table class="table">
        <thead>
            <tr>
            {% if is_granted ('ROLE_ADMIN') %}
                <th>Username</th>
            {% endif %}    
                <th>Name</th>
                <th>Firstname</th>
                {% if is_granted ('ROLE_ADMIN') %}
                <th>actions</th>
                {% endif %}
            </tr>
        </thead>
        <tbody>
        {% for technicien in techniciens %}
            <tr>
                {% if is_granted ('ROLE_ADMIN') %}
                <td>{{ technicien.username }}</td>
                {% endif %}
                <td>{{ technicien.name }}</td>
                <td>{{ technicien.firstname }}</td>

                {% if is_granted ('ROLE_ADMIN') %}
                <td>
                    <a href="{{ path('app_technicien_show', {'id': technicien.id}) }}">Détail</a>
                </td>
                {% endif %}
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% if is_granted ('ROLE_ADMIN') %}
    <a href="{{ path('app_technicien_new') }}">Ajouter un technicien</a>
    {% endif %}
{% endblock %}
